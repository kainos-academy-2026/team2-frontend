<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create your account</title>
    <link rel="stylesheet" href="/public/register.css" />
  </head>
  <body>
    <main class="page">
      <section class="shell" aria-label="Registration page">
        <aside class="brand-panel" aria-label="Brand information">
          <div>
            <div class="brand-lockup">
              <img
                class="brand-logo"
                src="/public/icons/Kainos_Logo.png"
                alt="Kainos logo"
              />
            </div>

            <p class="brand-blurb">
              <span>Join thousands of professionals finding their next opportunity.</span>
              <span>Create your account to get started.</span>
            </p>

            <div class="brand-copy">
              <img
                class="brand-icon"
                src="/public/icons/Register_Icon.png"
                alt="Icon"
              />
            </div>
          </div>
        </aside>

        <section class="form-panel">
          <div class="form-wrap">
            <p class="eyebrow">Create account</p>
            <h2>Register</h2>
            <p class="intro">
              Enter your details below to create an account.
            </p>

            {% if error %}
              <div class="alert alert-error" role="alert">{{ error }}</div>
            {% endif %}

            {% if success %}
              <div class="alert alert-success" role="status">{{ success }}</div>
            {% endif %}

            <form method="post" action="/register" novalidate>
              <div class="field">
                <label for="fullName">Full name</label>
                {% if errors and errors.fullName %}
                  {% for message in errors.fullName %}
                    <p class="field-error" id="fullName-error-{{ loop.index }}">{{ message }}</p>
                  {% endfor %}
                {% endif %}
                <input
                  id="fullName"
                  name="fullName"
                  type="text"
                  autocomplete="name"
                  placeholder="Jane Smith"
                  value="{{ values.fullName or '' }}"
                  class="{% if errors and errors.fullName %}input-error{% endif %}"
                  {% if errors and errors.fullName %}aria-invalid="true" aria-describedby="fullName-error-1"{% endif %}
                />
              </div>

              <div class="field">
                <label for="email">Email address</label>
                {% if errors and errors.email %}
                  {% for message in errors.email %}
                    <p class="field-error" id="email-error-{{ loop.index }}">{{ message }}</p>
                  {% endfor %}
                {% endif %}
                <input
                  id="email"
                  name="email"
                  type="text"
                  autocomplete="email"
                  placeholder="jane.smith@example.com"
                  value="{{ values.email or '' }}"
                  class="{% if errors and errors.email %}input-error{% endif %}"
                  {% if errors and errors.email %}aria-invalid="true" aria-describedby="email-error-1"{% endif %}
                />
              </div>

              <div class="field">
                <label for="password">Password</label>
                {% if errors and errors.password %}
                  {% for message in errors.password %}
                    <p class="field-error" id="password-error-{{ loop.index }}">{{ message }}</p>
                  {% endfor %}
                {% endif %}
                <input
                  id="password"
                  name="password"
                  type="password"
                  autocomplete="new-password"
                  placeholder="Create a secure password"
                  class="{% if errors and errors.password %}input-error{% endif %}"
                  {% if errors and errors.password %}aria-invalid="true" aria-describedby="password-error-1"{% endif %}
                />
              </div>

              <div class="field">
                <label for="confirmPassword">Confirm password</label>
                {% if errors and errors.confirmPassword %}
                  {% for message in errors.confirmPassword %}
                    <p class="field-error" id="confirmPassword-error-{{ loop.index }}">{{ message }}</p>
                  {% endfor %}
                {% endif %}
                <input
                  id="confirmPassword"
                  name="confirmPassword"
                  type="password"
                  autocomplete="new-password"
                  placeholder="Re-enter your password"
                  class="{% if errors and errors.confirmPassword %}input-error{% endif %}"
                  {% if errors and errors.confirmPassword %}aria-invalid="true" aria-describedby="confirmPassword-error-1"{% endif %}
                />
              </div>

              <div class="helper-row" aria-hidden="true">
                <span>Passwords must be at least 8 characters long and include at least one upper case, lower case and special character.</span>
              </div>

              <button class="register-button" type="submit">Register</button>
            </form>

            <p class="form-footer">
              Already have access? <a href="/">Return to the home page</a>.
            </p>
          </div>
        </section>
      </section>
    </main>
  </body>
</html>